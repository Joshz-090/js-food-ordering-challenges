// Challenge: Write a function that takes an array of numbers
// and returns a new array where each even number is multiplied by 3
// and each odd number is replaced with "odd"
// Output: ["odd", 6, "odd", 12, "odd", 18]

const nums = [1, 2, 3, 4, 5, 6];

function transformArray(arr) {
    return arr.map(num => {   // What is the use of map?
        // Write a condition here   use if and else
    });
}

const  = transformArray();  // Fix this — what is missing here?

console.log(result);
